#!/bin/sh

# Configures the Erlang distribution for use inside Docker.
# Single-node setup: bind to 127.0.0.1 inside the container.
# RELEASE_COOKIE is injected via env (docker-compose) — must be stable.

export RELEASE_DISTRIBUTION=name
export RELEASE_NODE="<%= @release.name %>@127.0.0.1"
