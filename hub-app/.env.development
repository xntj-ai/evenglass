# dev mode: base URLs intentionally blank so requests go through the
# vite proxy in vite.config.ts (localhost:5173 -> g2.xntj.tv). This keeps
# the browser on a single origin and avoids CORS preflight issues.
VITE_API_BASE=
VITE_WSS_BASE=
VITE_DEBUG_OVERLAY=false
